# データベース接続文字列（必須）
DATABASE_URL="postgresql://user:password@localhost:5432/shiftmanager"

# JWT 署名シークレット（本番必須。開発環境はデフォルト値で動作）
AUTH_SECRET=""

# Google Gemini API キー（任意。未設定時はルールベース生成にフォールバック）
GEMINI_API_KEY=""
